// Jasmine exports a set of global functions to perform tests: describe, it, expect, etc.
// These can be used only on .spec files
describe('App Component', () => {
  it('should pass sanity test', () => {
    expect(true).toBeTruthy();
  });
});
